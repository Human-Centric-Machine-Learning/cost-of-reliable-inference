# Runtime for the experiments. Config parsing uses stdlib tomllib (Python 3.11+).
numpy==2.5.2
matplotlib==3.11.1
pytest==9.1.1
# Only the data builders (build_datasets.py, build_rewards.py) need this.
huggingface-hub==1.28.0
# Only the notebooks in analysis/ need a kernel.
ipykernel==7.3.0
# Only analysis/main_paper_figure.py needs this.
pandas==2.2.3
